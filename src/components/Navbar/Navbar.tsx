import React from 'react'
import { NavbarRoot } from './Navbar.styles'

type NavbarProps = {
    leading: JSX.Element
    trailing: JSX.Element
}

export const Navbar = ({leading, trailing}: NavbarProps) => {
  return (
    <NavbarRoot>
        {}
    </NavbarRoot>
  )
}
